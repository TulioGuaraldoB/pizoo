version: '3.8'

services:
 postgres:
  image: 'postgres'
  environment:
   - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
   - POSTGRES_USER=${POSTGRES_USER}
   - POSTGRES_DB=${POSTGRES_DATABASE}

  ports:
   - 5432:5432    
